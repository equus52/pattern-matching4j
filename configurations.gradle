// configuration settings
// called one time from root project

ext {
  // product
  groupId = 'equus'
  artifactId = project.name
  productVersion = '0.1.0'
  
  // Java
  jdkVersion = '1.8'
  
  // maven
  releaseRepositoryDir = '../maven'
}