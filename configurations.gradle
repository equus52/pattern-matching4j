// configuration settings
// called one time from root project

ext {
  // product
  description = 'Java pattern matching library with Lambda expressions'
  groupId = 'com.github.equus52'
  artifactId = project.name
  productVersion = '0.2.0-SNAPSHOT'
  
  // Java
  jdkVersion = '1.8'
}