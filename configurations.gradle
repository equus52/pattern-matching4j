// configuration settings
// called one time from root project

ext {
  // product
  groupId = 'equus'
  artifactId = project.name
  productVersion = '0.1.1-SNAPSHOT'
  
  // Java
  jdkVersion = '1.8'
  
  // maven
  releaseRepositoryURL = uri('../maven')
}